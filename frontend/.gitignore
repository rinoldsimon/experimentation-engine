# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (ignore local overrides, keep the template)
.env
.env.*
!.env.example
# Not a secret -- just a fixed VITE_API_URL so `npm test`/CI don't need any
# local setup (see api.ts's startup check for why VITE_API_URL must exist).
!.env.test

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
