# Base URL the SPA uses for all API requests (see src/services/api.ts).
# Required: the app throws a startup error if this is unset. Point it at a
# different host/port if your API isn't running on the default one below.
VITE_API_URL=http://localhost:3000
